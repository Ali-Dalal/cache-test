extends layout

block content
    if !session.user
        a(href='/login') login
    if session.user
        h1 logged in as #{session.user.username}
        a(href='/logout') logout
    h1 Volcanic cache test
